from playwright.sync_api import Playwright


def pytest_configure(config):
    # To jest tu tylko po to, żeby plik istniał w repo jako "konfig"
    # Realne ustawienia dajemy w pytest.ini i fixtures.
    pass
